import { createContext } from 'react';

//Creating the context for future use with the help of React's hook
// Will be used by us in the future to create a component that will bring out with the context what we want
const mainContext = createContext();

export default mainContext;
